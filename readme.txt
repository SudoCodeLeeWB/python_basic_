Every module in the files must include start() function, which includes every element in the file. (to show the file output)

when new element is added, it needs to be included in main.py as import statement. 

